import type { AudioConfig } from './types';

export const audioConfig: AudioConfig = {
  masterVolume: 0.9,
  sfxVolume: 1,
  ambientVolume: 0.55,
  maxVoices: 24,
  refDistance: 2,
  maxDistance: 40,
  rolloffFactor: 1.4,
};
